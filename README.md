# Tinyproxy & Xray 安装教程

### 基础环境准备
`apt update && apt install git curl -y`

### 安装 Tinyproxy (假设文件夹内有 install.sh)
1. `cd Tinyproxy`
2. `chmod +x install.sh`
3. `./install.sh`

### 安装 Xray
1. `cd xray`
2. `chmod +x install.sh`
3. `./install.sh`
